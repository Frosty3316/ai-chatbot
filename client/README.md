# Dossier client

React + TypeScript + Vite UI. Production deploys this folder as the Vercel root.

- `npm run dev` — UI only
- `npm run dev:full` — UI plus `/api/chat` via `vercel dev`

See the repository README for architecture, quota, and portfolio data.
